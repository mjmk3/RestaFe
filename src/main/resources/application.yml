spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/restafe
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: ture
        dialect: org.hibernate.dialect.MySQL8Dialect
  session:
    jdbc:
      initialize-schema: always
#springdoc:
#  pathsToMatch: /**

server:
  port: 8448